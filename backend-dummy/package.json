{
  "name": "rateguard-backend-dummy",
  "version": "1.0.0",
  "private": true,
  "description": "Minimal upstream target for the RateGuard gateway proxy path (data plane helper).",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}